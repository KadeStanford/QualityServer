version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - node build.js
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
